apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: productinfo-api-hpa
  namespace: productinfo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: productinfo-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
